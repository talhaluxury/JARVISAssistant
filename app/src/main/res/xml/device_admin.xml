<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <!-- Intentionally minimal: we only need admin status itself (which makes
             Android disable the casual "Uninstall" button) - no lock-screen,
             wipe, or camera-disable policies are requested. -->
        <limit-password />
    </uses-policies>
</device-admin>
